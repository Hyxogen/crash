#include "commander.h"

char
	**param_pattern(t_minishell *sh, t_token *tok, char **str, size_t i)
{
	(void) sh;
	(void) tok;
	(void) str;
	(void) i;
	// TODO: implement
	sh_assert(0);
	return (NULL);
}
